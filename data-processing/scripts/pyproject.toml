[project]
name = "chess-stats-viz"
version = "0.1.0"
description = "Visualization scripts for chess game statistics"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.7.0",
    "torch>=2.0.0",
]

[tool.uv]
dev-dependencies = []
